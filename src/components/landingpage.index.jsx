import React from "react";
// import pictures from "../../pictures.json";

const LandingPage = ({id, image}) => {

  return (
    <div className="card">
    <div className="img-container">
      <img alt={id} src={image} />
    </div>
    {/* <span onClick={() => props.sh(props.id)} > */}
      𝘅
    {/* </span> */}
  </div>
  )
}

export default LandingPage;
